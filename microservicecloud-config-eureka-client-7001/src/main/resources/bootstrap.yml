spring:
  cloud:
    config:
      name: microservicecloud-config-eureka-client  #需要从GitHub上读取的资源名称，没有后缀名
      profile: dev
      label: master
      uri: http://config-3344.com:3344  #本微服务启动后先去找3344号服务，通过SpringCloudConfig获取GitHub的服务地址